/**
 * Services Index
 * 
 * This file exports all services used throughout the application.
 * For now, we're using mock data, but this structure allows for easy
 * integration with real API endpoints in the future.
 * 
 * Example service structure:
 * 
 * export const violationService = {
 *   getAll: async () => { ... },
 *   getById: async (id: string) => { ... },
 *   create: async (data) => { ... },
 *   update: async (id, data) => { ... },
 *   delete: async (id) => { ... }
 * };
 */

// Service exports will go here
// Currently using mock data from data/mockData.ts
